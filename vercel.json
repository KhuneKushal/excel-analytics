{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/excel-analytics",
  "builds": [
    { "src": "dist/excel-analytics/server/server.mjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/excel-analytics/server/server.mjs" }
  ]
}